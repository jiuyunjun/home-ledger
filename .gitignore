# Local environment
.env
.env.local

# Firebase deploy cache
.firebase/

# Service account keys — never commit
*.json.key
firebase-deploy-key.json

# Claude Code local settings
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Go
/api/bin/
*.exe

# Firebase / GCP service account keys — never commit
/api/credentials/
/api/*.json

# Node
node_modules/
.next/
dist/

# IDE
.idea/
.vscode/

# VSCode "Local History" extension — keeps timestamped copies of edited files
# (including .env), do NOT commit
.history/
